# Tasks: ui-baseline-fix

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~260 reviewable + ~200 generated = ~460 total |
| 400-line budget risk | Medium |
| Chained PRs recommended | No |
| Suggested split | Single PR (generated components inflate diff but are not reviewable) |
| Delivery strategy | exception-ok |
| Chain strategy | size-exception |

Decision needed before apply: Yes — resolved as exception-ok (maintainer accepts size:exception for generated shadcn boilerplate)
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: Medium

### Justification
Actual code changes (globals.css + 3 component files) = ~260 lines. The remaining ~200 lines are shadcn boilerplate components (badge, textarea, skeleton, alert) generated by CLI. Reviewers only need to review ~260 lines — well within the 400-line budget. Safe for single PR with `size:exception` acknowledgment for generated boilerplate.

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | globals.css restructure + shadcn install | PR 1 (base: feat/productos-ui) | Foundation — must come first |
| 2+3 | product-table.tsx + product-form-dialog.tsx | PR 1 | Both are products page refactors |
| 4 | loading.tsx skeleton | PR 1 | Same branch, minimal risk |

## Phase 1: Infrastructure

- [x] 1.1 **Install 4 missing shadcn components**: Ran `npx shadcn@latest add badge textarea skeleton alert -y` — generated `components/ui/{badge,textarea,skeleton,alert}.tsx`
- [x] 1.2 **Restructure globals.css**: Replaced entire file with new structure — `@import "tw-animate-css"`, `@custom-variant dark`, shadcn `@theme inline` tokens (18 `--color-*` + 4 `--radius-*`), brand colors preserved, `:root` / `.dark` OKLCH blocks. Deleted old `@media (prefers-color-scheme: dark)` block. Removed legacy `:root` compat vars.

## Phase 2: Core Implementation

- [x] 2.1 **Fix product-table.tsx**:
  - Replaced local `cn()` with `import { cn } from "@/lib/utils"` + imported `Badge`, `Alert`, `AlertTitle`, `AlertDescription`
  - Replaced viewer notice `<div>` with `<Alert>` + `<Eye>` + `<AlertTitle>` + `<AlertDescription>`
  - Replaced error banner `<div>` with `<Alert variant="destructive">` + `<AlertCircle>` + `<AlertTitle>Error</AlertTitle>` + `<AlertDescription>` + Reintentar button with `<RotateCcw data-icon="inline-start" />` outside Alert
  - Replaced badge `<span>` with `<Badge variant="default">` (Activo) and `<Badge variant="secondary">` (Inactivo) with `data-icon="inline-start"`
  - Fixed `border-primary text-primary` → `border-brand-accent text-brand-accent`
- [x] 2.2 **Fix product-form-dialog.tsx**:
  - Replaced `<textarea>` with `<Textarea className="h-20" />`; imported from `@/components/ui/textarea`
  - Replaced `space-y-4` (form) with `flex flex-col gap-4`
  - Replaced all instances of `space-y-1.5` with `flex flex-col gap-1.5` (Descripción one required a follow-up edit after replaceAll missed it due to content shifting)
- [x] 2.3 **Fix loading.tsx**: Imported `Skeleton` + `Table` components; replaced all `animate-pulse` divs with `<Skeleton>` preserving exact dimensions; replaced native `<table>`/`<thead>`/`<tbody>` with `<Table>`/`<TableHeader>`/`<TableBody>` matching real table structure

## Verification

- [x] 3.1 `npx tsc --noEmit` — passes with no new errors (all errors are pre-existing in compras module)
- [x] 3.2 `pnpm lint` — passes with no new errors (all errors/warnings are pre-existing in unrelated files)
- [ ] 3.3 Status column renders Badge with correct colors (green active, muted inactive) — visual check needed
- [ ] 3.4 Description field uses Textarea with proper focus ring and resize — visual check needed
- [ ] 3.5 Loading page shows Skeleton shapes matching real table dimensions — visual check needed
- [ ] 3.6 Viewer notice renders as styled Alert (not raw Tailwind colors) — visual check needed
- [ ] 3.7 "Mostrar inactivos" button highlights with brand accent, not black — visual check needed
- [ ] 3.8 Dialog open/close animates (tests tw-animate-css import) — visual check needed
- [ ] 3.9 Icon attributes use `data-icon="inline-start"` (no manual size classes on RotateCcw) — confirmed via code review

## Dependency Order

Task 1 (Phase 1) must run first — components must be installed and CSS variables defined before files can import them. Tasks 2.1, 2.2, 2.3 are independent of each other (all depend only on Task 1). Verification runs last.
